"use client";

import { videos } from "./data/data";

export default function SearchBar() {
  
//  const ListItems = videos.filter((list) => {
//     list.id.indexOf() > 
//   }); 

  return (
    <>
      <form className="flex space-x-2">
        <input placeholder="search..."  />
      </form>
    </>
  );
}
